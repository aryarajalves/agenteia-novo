# Regras de Negócio - Sistema de Agentes Jaime

> 🛠️ **Nota Técnica:** A URL `https://backendagente.aryaraj.shop` é um túnel Cloudflare para o ambiente de desenvolvimento local. Trate-a como localhost.

## 📋 Status de Decisões

## 🌐 Conectividade e URLs
- **Redirecionamento Global:** Todas as URLs geradas pelo sistema (Webhooks, Widgets de Chat, Callbacks de OAuth) devem obrigatoriamente utilizar o endereço do túnel (Cloudflare) configurado em `BACKEND_URL` / `VITE_API_URL` em vez de `localhost`.
- **Sincronização de Ambiente:** As variáveis de ambiente `BACKEND_URL` e `VITE_API_URL` no arquivo `.env` devem ser mantidas em sincronia para garantir que o frontend e o backend falem a mesma "língua" pública.

- [ ] [NOVO] Como devemos tratar registros antigos na tabela `webhook_events` (Histórico de Disparos) que não possuem o campo `message_type`? Atualmente, eles exibem um ícone padrão de texto (📝). Devemos manter assim ou exibir um aviso de "Legado/Desconhecido"?

## 🔗 Slugs e Integrações
- **Plataforma NATIVA de Chat:** O sistema utiliza **exclusivamente** o chat do **ZapVoice** (o Chatwoot foi descontinuado do projeto). Todos os webhooks, envio de mensagens e sincronização de etiquetas utilizam as rotas da API do ZapVoice (`/api/chat/conversations/...`).
- **Obrigatoriedade:** Slugs não podem ser deixados em branco durante a criação de uma integração.


## 👥 Gestão de Usuários
- [x] O sistema utiliza um fluxo de convites para novos usuários.
- [x] Administradores podem gerenciar permissões e agentes.

## 🤖 Agentes e Automação
- [x] Suporte a RAG via pgvector.
- [x] [NOVO] (14/07/2026) Edição de item de conhecimento (`PUT /knowledge-items/{id}`) deve recalcular o vetor (embedding)?
  - Resposta: Sim, sempre recalcular ao salvar edição, independente de qual campo mudou (antes só recalculava se a Pergunta mudasse). O vetor continua sendo gerado só a partir do campo "Pergunta" (arquitetura de busca RAG atual não embeda Resposta/Metadado) — se no futuro quiser que o vetor reflita também a Resposta, isso muda a relevância da busca em todas as bases e deve ser confirmado antes de implementar.
- [x] [NOVO] (04/09/2026) Variações da Pergunta na Base de Conhecimento: Cada item de conhecimento pode conter múltiplas variações / formas alternativas da mesma pergunta (armazenadas em JSON na coluna `question_variations` da tabela `knowledge_items`). O vetor semântico (embedding) do item é gerado a partir da Pergunta principal concatenada com suas variações em quebras de linha, ampliando a similaridade semântica para todas as variações cadastradas. A busca FTS e o catálogo de referência de Query Enrichment no Pre-Router também indexam as variações para máxima assertividade no alinhamento de dúvidas.
- [x] [NOVO] (14/07/2026) Descoberto que `get_embedding` (backend/services/rag/providers.py) engolia qualquer erro (chave da OpenAI ausente/inválida, falha de rede, quota, etc.) e retornava `None` silenciosamente — o item era criado/editado normalmente, só que sem vetor, sem nenhum aviso. Também não existe `backend/.env` neste projeto (só `.env.example`), que é a causa mais provável de itens sem vetor. O que fazer quando a geração do vetor falhar?
  - Resposta: Bloquear a criação/edição. Agora `get_embedding` levanta `EmbeddingGenerationError` em vez de devolver `None`, e os endpoints `POST /knowledge-bases/{kb_id}/items` e `PUT /knowledge-items/{id}` retornam 502 com mensagem clara e não salvam o item se o vetor não puder ser gerado. Pendente: verificar se `OPENAI_API_KEY` está de fato configurado no ambiente real (não encontrado neste projeto).
- [x] Integração com Google Calendar via OAuth.
- [x] Bug encontrado e corrigido (14/07/2026): a sincronização de etiquetas da ferramenta `transferir_suporte_humano` (`agent_core/tools/handlers/chatwoot.py`) nunca funcionou de verdade neste projeto — o código importava um módulo `chatwoot_utils` que jamais existiu aqui (resquício de uma versão anterior baseada em Chatwoot, antes da migração para ZapVoice) e usava variáveis de ambiente `CHATWOOT_URL`/`CHATWOOT_API_TOKEN` que também não existem. O `ImportError` era engolido por um `except Exception` genérico, então a automação sempre reportava "Etiquetas Adicionadas/Removidas" no pipeline sem nunca de fato chamar a API do ZapVoice. Corrigido para usar `zapvoice_utils.sync_conversation_labels` com `zapvoice_url`/`zapvoice_api_token` do `WebhookConfigModel`, igual ao resto do projeto. Confirmado via `git diff` que esse arquivo não foi alterado durante a remoção do painel "Suporte Humano" — o bug é anterior e não tem relação com aquela mudança. Testes `test_support_phone_mapping.py` e `test_transferir_suporte_humano.py` atualizados para refletir o ZapVoice em vez do Chatwoot legado.
  - Pendente de confirmação do usuário: no webhook em uso, o campo "Suporte Humano" → "Remover" e "Adicionar" (EditWebhookModal) está configurado com a MESMA etiqueta (`robo`) nos dois campos, o que é contraditório (adicionar e remover a mesma tag ao mesmo tempo). Isso é intencional ou é engano de configuração? Recomendo revisar essa tela.
- [x] [NOVO] O painel "Suporte Humano" (sidebar, página, endpoints `/support-requests`) deve ser removido — a ferramenta de IA `transferir_suporte_humano` deve continuar funcionando (cria o registro em `SupportRequestModel` e pausa a automação), mas sem nenhuma interface para marcar como resolvido/devolver ao robô pelo sistema?
  - Resposta (14/07/2026): Sim, remover tudo sem substituto — outro projeto do usuário cuida do suporte humano. Removidos: item da sidebar e rota `/support` (frontend), componentes `SupportDashboard` e `PublicSupportView` (movidos para `codigo_obsoleto/`, não apagados — usuário pediu para arquivar em vez de excluir), router `backend/api/routers/support.py` e seu `include_router` em `api/main.py` (movido para `codigo_obsoleto/backend/`), testes que cobriam só essas partes removidas (`test_support.py`, `test_support_resolve.py`, `test_support_ids.py`, `test_support_handoff_logic.py`, `verify_support_deletion.py`, também movidos). Mantidos intactos: `SupportRequestModel` (models.py), a ferramenta `transferir_suporte_humano`/`handle_chatwoot_handoff` (agent_core/tools/handlers/chatwoot.py) e seu registro em `database/seeds.py` — a IA continua transferindo e pausando a automação normalmente, só não há mais painel/endpoint para gerenciar a fila.
- [x] [NOVO] (10/08/2026) Quando o usuário enviar um agradecimento ou encerramento (ex: "Obrigada ❤️", "Perfeito ❤️", "Valeu", "👍") pela SEGUNDA vez (ou subsequente) após o assistente já ter enviado uma resposta de encerramento ("Por nada! Se precisar de mais alguma coisa, é só chamar"), a automação do Pre-Router deve ser silenciada (não enviar nenhuma resposta ao cliente), registrando o motivo na pipeline do evento (`"🤫 Automação Silenciada - Agradecimento Recorrente"`) para evitar envio infinito de mensagens e loop.
- [x] [NOVO] (10/08/2026) Se o usuário apenas declarar que tem dúvidas ou citar um tópico genérico sem fazer uma pergunta objetiva (ex: "Não finalizei tive umas duvida", "Sobre a máquina", "tenho dúvidas"), o sistema é terminantemente PROIBIDO de enviar uma lista de dúvidas mais comuns (FAQ) não solicitadas ou responder perguntas que o cliente não fez. O Pre-Router e o Agente devem obrigatoriamente perguntar educadamente qual é a dúvida específica do usuário (ex: "Quais são as suas dúvidas sobre a máquina? Me conte o que você gostaria de saber para que eu possa te ajudar!").
- [x] [NOVO] (10/08/2026) Quando o usuário enviar mensagens com múltiplas dúvidas/perguntas em linhas separadas (ex: "O curso é on-line \n De onde vc é"), o Pre-Router é TERMINANTEMENTE PROIBIDO de omitir/descartar perguntas curtas como "De onde vc é". O sistema deve obrigatoriamente preservar TODAS as perguntas no array `lista_perguntas_extraidas` e concatenar todas elas em `perguntas_extraidas` unidas por `\n`, para que o RAG e o Agente Principal recebam e respondam a todas as dúvidas enviadas no mesmo turno.
- [x] [NOVO] (10/08/2026) Quando o cliente informar que comprou/pagou mas "ainda não acessei as aulas", "não assisti ainda" ou "não entrei ainda", a IA é TERMINANTEMENTE PROIBIDA de presumir erro técnico de acesso e PROIBIDA de declarar em texto que vai transferir ou encaminhar para outro setor. O sistema deve apenas responder de forma acolhedora parabenizando a compra e incentivando o acesso quando o aluno puder.
- [x] [NOVO] (10/08/2026) Ao reescrever e enriquecer dúvidas gerais sobre o curso (ex: "Na vdd, gostaria de saber sobre os cursos"), o Pre-Router é TERMINANTEMENTE PROIBIDO de inventar termos de busca de estoque/catálogo como "estão disponíveis?" ou injetar nomes de marcas não ditos. A mensagem deve ser padronizada diretamente para "Como funciona o curso de remoção de tatuagem?", garantindo que a busca vetorial (RAG) encontre o item de conhecimento correto sem acionar indevidamente a ferramenta `registrar_duvida_sem_resposta`.
- [x] [NOVO] (10/08/2026) Quando o assistente oferecer opções ou tópicos no turno anterior (ex: "Se quiser mais informações sobre o curso, sobre pagamentos ou se quiser o link de compra, é só me avisar") e o usuário responder com confirmação ou demonstração de interesse (ex: "Gostaria", "Quero", "Sim", "Gostaria sim", "Aceito"), o Pre-Router é OBRIGADO a expandir "Gostaria" para TODAS as opções oferecidas ("Como funciona o curso?", "Quais são as formas de pagamento?", "Qual é o link de compra / inscrição?"), para que a busca RAG e a IA enviem todas as respostas solicitadas no mesmo turno.
- [x] [NOVO] (10/08/2026) Se o usuário relatar 3 ou mais vezes na mesma conversa/sessão que está com dificuldades para pagar ou comprar (ex: "não estou conseguindo pagar com cartão", "consigo pagar por outro link?", "estou desde ontem tentando", "cartão recusado"), o Pre-Router e a IA são OBRIGADOS a acionar automaticamente a ferramenta `transferir_suporte_humano` com a justificativa de "Dificuldade de pagamento", pausando o robô e transferindo a conversa para o suporte humano finalizar a venda.
- [x] [NOVO] (03/09/2026) Limite e Transbordo por Dúvidas Sem Resposta: O limite de dúvidas sem resposta na mesma conversa antes de acionar `transferir_suporte_humano` é configurável pelo painel do agente (`unanswered_handoff_limit`, padrão 2). Caso o usuário desative a opção ou defina limite 0, o robô NUNCA chamará o suporte humano por dúvidas não respondidas, continuando a atender em todos os turnos. Essa desativação aplica-se EXCLUSIVAMENTE ao fallback de dúvidas sem resposta: se o cliente solicitar explicitamente suporte humano, cancelamento ou reembolso, a ferramenta `transferir_suporte_humano` continuará sendo chamada normalmente. O usuário também pode configurar uma diretriz/modelo de resposta personalizada (`unanswered_question_prompt`) para a ferramenta `registrar_duvida_sem_resposta`.
- [x] [NOVO] (31/07/2026) Pedidos de cancelamento, devolução ou reembolso de compras/cursos devem acionar a ferramenta `transferir_suporte_humano` mesmo que o cliente não peça explicitamente por atendente humano. Além disso, a IA está terminantemente proibida de declarar em texto que vai transferir ou chamar a equipe sem acionar a ferramenta `transferir_suporte_humano` no mesmo turno.
- [x] [NOVO] (14/07/2026) O prompt do Pre-Router (IA de triagem que decide saudação/ferramenta/RAG/roteamento antes do agente principal) era 100% hardcoded em `agent_core/logic/pre_router.py`, sem armazenamento em banco nem UI. O usuário pediu para poder ver e editar esse prompt na mesma tela do Editor de Prompt. Como isso deveria ser exposto, dado que o template tem um rodapé com schema JSON obrigatório (`chamada_ferramenta`, `precisa_rag`, etc.) que, se removido/quebrado pelo usuário, faria o Pre-Router parar de funcionar corretamente para aquele agente?
  - Resposta (decisão de implementação, sem bloquear no usuário por ser puramente técnica): Adicionada 3ª aba "🧭 Pre-Router" no Editor de Prompt (junto de Estático/Dinâmico), ligada à nova coluna `agent_config.pre_router_prompt` (nullable). Para minimizar o risco de o agente "quebrar" o Pre-Router: (1) o rodapé com o schema JSON de retorno (`PRE_ROUTER_JSON_FOOTER` em `pre_router.py`) NUNCA é customizável — é sempre concatenado por código após o texto do usuário, então a IA sempre retorna JSON estruturado válido independente do que for editado; (2) o texto customizável aceita os mesmos placeholders do template padrão (`{tools_desc}`, `{agents_desc}`, `{initial_msg}`, `{main_system_prompt}`, `{date_context}`, etc.) via `.format_map` com fallback seguro (chave ausente não quebra, mantém o texto literal); (3) um aviso visível foi adicionado na aba avisando para não remover `{tools_desc}`/`{agents_desc}`; (4) botão "Restaurar Padrão" busca `GET /agents/pre-router-default-prompt` para repopular com o template original a qualquer momento. Se o campo estiver vazio, o backend usa o `DEFAULT_PRE_ROUTER_PROMPT_TEMPLATE` automaticamente (comportamento idêntico ao anterior, sem quebra para agentes existentes).
  - **Aviso de infraestrutura (regra `integridade-banco.md`):** a coluna `pre_router_prompt` só é criada automaticamente em bancos novos (via `create_all`). Se este projeto já roda em produção com banco existente, rodar `alembic upgrade head` (a migration `e3f4a5b6c7d8` já existia órfã na cadeia, só faltava o mapeamento em `models.py`) ou aplicar a coluna manualmente.
- [x] [NOVO] (19/08/2026) Quando o cliente enviar mensagens curtas de confirmação ou encerramento passivo (ex: "Ta bom", "Tá bom", "Ta bem", "Tudo bem", "Ok", "Entendi", "Combinado", "Certo", "Beleza", "Ótimo", "Maravilha") após receber informações ou links de checkout, o Pre-Router e o módulo de Query Enrichment são TERMINANTEMENTE PROIBIDOS de reescrever ou expandir essas expressões como se fossem novos pedidos de compra/formas de pagamento/links. O sistema deve classificar como confirmação (eh_saudacao = true) e responder com uma mensagem acolhedora e conclusiva (ex: "Combinado! Se precisar de qualquer ajuda, estou por aqui. 😊") sem reenviar links ou listar formas de pagamento repetidas.
- [x] [NOVO] (26/08/2026) Quando o cliente enviar uma imagem no WhatsApp (anúncio, criativo, print de dúvida ou comprovante): O agente deve atender diretamente em 1ª pessoa no WhatsApp, com tom acolhedor e consultivo. É TERMINANTEMENTE PROIBIDO agir como copiloto ou falar em 3ª pessoa ("Segue sugestão de resposta para enviar a..."). Se a imagem for um anúncio/criativo da empresa enviado sem texto, o agente deve puxar conversa sobre o tema do anúncio de forma amigável em vez de empurrar links de pagamento de supetão.
- [x] [NOVO] (26/08/2026) Tratamento Especializado de Imagens (OCR de Comprovantes, Produtos e Prints Técnicos): Quando o cliente enviar comprovantes de pagamento (PIX/transferência), a IA deve reconhecer os dados do comprovante (valor/favorecido), agradecer o envio em 1ª pessoa e confirmar que a validação está em andamento, sem reenviar links de checkout. Para fotos de produtos/equipamentos com defeito e prints de erro, a IA deve acolher com empatia consultiva e orientar com clareza a solução ou próximo passo.
- [x] [NOVO] (02/09/2026) Quando o cliente enviar mensagens de encerramento coloquial ou declaração de que não restam dúvidas (ex: "Não era só isso mesmo", "Era só isso mesmo", "Só isso mesmo", "Era só isso", "Não, só isso", "Não preciso de mais nada", "Nada mais", "Por enquanto é só"): O Pre-Router é TERMINANTEMENTE PROIBIDO de classificar como mensagem vaga e PROIBIDO de definir `precisa_esclarecimento: true` com perguntas do tipo "O que mais você precisa?". O sistema deve obrigatoriamente classificar como encerramento/agradecimento educado (`eh_agradecimento: true`, `eh_saudacao: true`, `precisa_esclarecimento: false`) e fornecer uma despedida conclusiva e acolhedora (ex: "Perfeito! Fico à disposição se precisar de qualquer outra informação ou se tiver alguma dúvida. Bons estudos e até logo! 😊"). Se for um encerramento consecutivo reincidente, deve silenciar a automação sem enviar mensagens repetidas.
- [x] [NOVO] (02/09/2026) Visualização e Mineração de Dúvidas de Leads para o Cache Semântico: O painel de Cache Semântico conta com a sub-aba "📥 Dúvidas dos Leads (Mineração)". Nela, são listadas todas as perguntas e mensagens reais recebidas pelos webhooks do agente, com filtros por "Sem Cache (Candidatas a Economia)", "No Cache (Já Atendidas)" e busca livre. Ao lado de cada dúvida de lead, o botão "⚡ Adicionar ao Cache" abre o formulário de cadastro já pré-preenchido com a pergunta do lead e a resposta gerada pelo robô para aprovação imediata em 1 clique.
- [x] [NOVO] (03/09/2026) Continuação após Responder à 1ª Dúvida do Usuário (1ª Mensagem com Pergunta): Quando a primeira mensagem enviada pelo lead já contiver uma dúvida/pergunta direta (em vez de uma simples saudação), o robô responde com clareza à dúvida do usuário e, no encerramento dessa 1ª resposta, anexa a pergunta/mensagem de continuação configurada pelo gestor no painel (`initial_question_message`), removendo automaticamente perguntas genéricas redundantes de fechamento geradas pela LLM.
- [x] [NOVO] (04/09/2026) Preservação de Custos e Tokens ao Deletar Contatos: Ao excluir um lead/contato no painel de Contatos Capturados (ou via deleção por palavras-chave), o sistema limpa as memórias conversacionais e eventos da sessão (`user_memory`, `session_summaries`, `webhook_events`), mas **preserva integralmente** todos os registros na tabela `interaction_logs`. Isso garante que as métricas de Total de Interações, consumo de tokens e Custo Estimado (R$) exibidas no Dashboard e nos relatórios permaneçam auditáveis e nunca sofram redução ou perda histórica.
- [x] [NOVO] (04/09/2026) Acionamento Único da Ferramenta `lead_qualificado` e Supressão para Leads Já Qualificados: A ferramenta `lead_qualificado` deve ser acionada EXATAMENTE UMA ÚNICA VEZ durante todo o ciclo de vida do atendimento, exclusivamente no turno em que a última pergunta de qualificação do funil for respondida pelo lead. Para contatos que já possuem qualificação concluída no banco de dados (`respostas_qualificacao`), a ferramenta é suprimida do esquema da IA e o agente é instruído a não repetir perguntas e focar 100% no atendimento consultivo e fechamento. O Pre-Router não executa antecipadamente a ferramenta de qualificação.
- [x] [NOVO] (04/09/2026) Regra de Permissão em Dois Passos para Ação/Pergunta Final de Fechamento: Quando a diretriz configurada em 'Pergunta / Ação Final Pós-Qualificação (Fechamento)' pedir para fazer uma pergunta de permissão ou interesse em receber link/oferta (ex: 'Pergunte se eu posso enviar o link do curso para ele...'), a IA deve realizar APENAS A PERGUNTA no turno imediato da qualificação, sendo estritamente proibido enviar a URL/checkout junto com a pergunta. O link deve ser enviado apenas no turno seguinte, quando o lead confirmar (ex: 'Sim', 'Pode enviar'). Para contatos já qualificados, a diretriz com o link é mantida no system prompt para permitir o envio formatado após a confirmação.
- [x] [NOVO] (04/09/2026) Disparo Condicional da Pergunta / Ação Final por Lead Score (`qualification_final_action_trigger`): O gestor pode escolher quando a Pergunta / Ação Final Pós-Qualificação será acionada pelo robô: `🌟 Sempre / Todas` (padrão), `🔥 Apenas Quente`, `🔥⚡ Quente ou Morno`, `⚡ Apenas Morno` ou `❄️ Apenas Frio`. Caso o lead não corresponda à temperatura selecionada no funil, o robô encerra o fluxo cordialmente sem ofertar fechamento nem enviar links.
- [x] [NOVO] (04/09/2026) Etiquetagem Condicional à Qualificação de Fato & Uso Exclusivo do Dropdown: O lead só deve receber as etiquetas de qualificação (tanto no banco local `leads` quanto no ZapVoice) caso ele se qualifique de fato (classificação diferente de `Frio ❄️` / desqualificado, respeitando o gatilho configurado). Leads frios ou desqualificados não são etiquetados com tags de qualificação. Além disso, a aplicação utiliza estritamente as etiquetas presentes no dropdown (`qualification_labels`), removendo ativamente qualquer tag legada estática (ex: `qualificado`).
- [x] [NOVO] (08/09/2026) Redefinição de Etiquetas no ZapVoice ao Deletar Contatos no AgentFlow: Ao excluir um lead/contato no AgentFlow (manualmente pelo painel de contatos capturados, CRM, exclusão em lote ou via auto-deleção por palavra-chave como `#resetar`), o sistema sincroniza e redefine as etiquetas da conversa no ZapVoice para as etiquetas padrão configuradas (`delete_labels` ou fallback para `labels_on_message`). O campo de seleção e digitação das etiquetas padrão fica permanentemente visível e funcional na aba 'Segurança' -> 'Exclusão de Contatos' da integração.
- [x] [NOVO] (08/09/2026) Continuidade do Funil de Qualificação com Respostas Negativas ('Não Tenho Dúvidas'): Quando o lead responder com negação ('Não', 'Não tenho dúvidas', 'Nenhuma', etc.) após o assistente perguntar sobre dúvidas, o robô acolhe com simpatia e dá sequência ao atendimento introduzindo estritamente a próxima etapa pendente do funil de qualificação configurado no agente. É terminantemente proibido o uso de exemplos hardcoded no prompt do sistema e proibido inventar perguntas fora das etapas configuradas no funil do agente.
- [x] [NOVO] (08/09/2026) Diagnóstico de Raciocínio da Resposta no Pipeline: No modal de Pipeline de Automação de Webhooks, o card 'Resposta gerada pelo agente' conta com a ferramenta '🔬 Por que essa resposta? (Ver Motivo & Passo a Passo)'. Ao clicar, o sistema analisa os fatores, diretrizes do prompt do sistema, contexto da mensagem do lead e resposta gerada, discriminando de forma transparente: (1) o motivo do acolhimento/primeira parte da mensagem, (2) o motivo da pergunta ou condução final, (3) a linha de raciocínio numerada passo a passo que levou a IA a essa decisão, e (4) as regras específicas do prompt aplicadas. O resultado é persistido nos metadados do evento para consultas subsequentes instantâneas sem retrabalho da LLM.
- [x] [NOVO] (14/09/2026) Padrões dos Filtros de RAG (Multi-Query e Expansão de Contexto Pai): O filtro MULTIQUERY (geração de variações da pergunta para ampliar a busca vetorial e FTS) passa a ser ATIVADO por padrão (`rag_multi_query_enabled = True`), e o filtro PARENTEXPANSION (Expansão de Contexto Pai) passa a ser DESATIVADO por padrão (`rag_parent_expansion_enabled = False`), tanto no Simulador de Busca das Bases de Conhecimento (`SimulatorBlock`), quanto nas configurações de agentes (`agent_config`), formulários do painel e pipelines de execução do RAG.
- [x] [NOVO] (15/09/2026) Funis de Conversão por Dúvida (Áudios Humanizados PTT & Sequências Pré-Configuradas): Para perguntas frequentes e estratégicas do lead (ex: "como funciona o curso de vcs?"), o gestor pode cadastrar Funis por Dúvida dedicados na aba "🎯 Funis por Dúvida" do painel do agente. (1) Gatilho Semântico: Acionado via similaridade de cosseno de embeddings OpenAI sobre a pergunta principal e variações cadastradas com limiar configurável (padrão 82%). (2) Frequência: Suporta "Apenas 1 única vez por lead" (dispara na 1ª ocorrência; se perguntar novamente, a IA responde normalmente via RAG) ou "Sempre que o lead perguntar". (3) Sequência de Passos: Passos encadeados com delays em segundos (áudio PTT com upload S3/MinIO ou URL, mensagens de texto, imagens, vídeos). (4) Continuidade Conversacional: A transcrição do áudio alimenta a memória da conversa para que a IA nos turnos seguintes mantenha total alinhamento. (5) Custo Zero de LLM: Responde instantaneamente sem consumo de tokens de geração.
- [x] [NOVO] (17/09/2026) Declaração de Compra Informada pelo Lead & Supressão de Respostas Estáticas (shortcut-logic): Toda e qualquer mensagem em que o lead declarar que comprou ou pagou o curso/produto (ex: "já comprei", "oiee comprei o curso, mas não chegou meu acesso no meu email, como faço?") é TERMINANTEMENTE PROIBIDA de ser interceptada por atalhos com respostas prontas estáticas (shortcut-logic) como parabéns enlatados. A mensagem deve ser integralmente analisada e respondida pelo modelo de IA (respeitando o prompt, persona e RAG do agente para tirar dúvidas e resolver problemas de acesso). O Pré-Router mantém apenas a sinalização interna (eh_compra_informada = true) para que a automação aplique a etiqueta de aluno no ZapVoice e cancele as réguas de follow-up de vendas no banco de dados.
- [x] [NOVO] (17/09/2026) Importação em Massa de Conversas e Mensagens do ZapJords (Custo Zero de IA): O botão "📥 Importar do ZapJords" no modal de Contatos Capturados consulta a API do ZapJords (`GET /api/chat/conversations?status=all` e `GET /api/chat/conversations/{id}/messages?limit=0`) para sincronizar todo o histórico de atendimento. Se o contato ainda não existir, é criado no banco local com `followup_step = -1` (régua de vendas concluída/pausada para não disparar follow-ups retroativos em conversas antigas). Mensagens novas são ingeridas na tabela `webhook_events` com deduplicação por `mensagem_id` (`dono = 'usuario'` para contatos e `dono = 'agente'` para respostas reais de atendentes/robô). É terminantemente proibido importar badges e eventos de sistema do ZapJords (ex: 'Marcador(es) adicionado(s)', 'O atendente Super Admin adicionou/removeu marcador', '🚀 Funil iniciado', `sender_type = 'system'`, `message_type = 'funnel_event'`). Nas respostas do agente, são importadas estritamente as respostas textuais reais trocadas com o cliente, alimentando a memória da conversa da IA sem ruídos e sem consumir chamadas ou tokens de LLM. O processo roda de forma assíncrona com feedback visual em tempo real via WebSocket.


## 📝 Perguntas e Decisões Pendentes
- [x] [NOVO] Existe algum ambiente onde o uso de `localhost:8002` ainda seja obrigatório para o frontend em vez do túnel?
  - Resposta: No ambiente de desenvolvimento local, o frontend acessa o backend via `localhost:8002`. O erro de agentes sumindo foi causado por um mismatch (estava 8000 no config.js).
- [x] [NOVO] As rotas `/agents/{id}/generate-description` e `/agents/{id}/duplicate` foram removidas ou ainda precisam ser implementadas? Elas constam nos testes e em partes do frontend, mas não estão no backend atual (causando 404).
  - Resposta (05/09/2026): Ambas as rotas foram implementadas no `backend/api/routers/agents.py`. A rota `POST /agents/{agent_id}/generate-description` atende diretamente ao botão "✨ Sincronizar IA" presente na aba de agente da integração (resumindo o papel e especialidade do agente com IA para orientar o roteamento do Pre-Router), e a rota `POST /agents/{agent_id}/duplicate` clona agentes e suas ferramentas/bases associadas.
- [x] [NOVO] Se o áudio/imagem for permitido nas configurações, a automação deve realmente PARAR e apenas avisar o tipo, ou ela deveria tentar processar (ex: transcrever áudio ou descrever imagem) no futuro?
  - Resposta: No futuro deve processar (baixar e transcrever), mas por enquanto deve apenas parar e identificar o tipo.
- [x] [NOVO] Quando uma mídia for bloqueada (ex: vídeo ou documento), devemos enviar uma mensagem automática ao cliente final avisando que o formato não é aceito, ou apenas registrar isso no log interno da pipeline?
  - Resposta: Apenas registrar no log interno da pipeline, não enviar nada ao cliente final.
- [x] Qual deve ser o tempo padrão de expiração do Toast (atualmente 3 segundos)?
  - Resposta: 5 segundos.
- [x] Deseja que ao expandir um contato, todos os outros se fechem automaticamente (comportamento de Accordion)?
  - Resposta: Sim, deve fechar os outros ao abrir um novo.
- [ ] [NOVO] Deseja que a automação de retorno ao robô envie um log específico para o painel de histórico do agente informando que a IA reassumiu o controle?
- [ ] [NOVO] Quem é o 'Mateus' mencionado nos testes? Ele é um membro da equipe de suporte, um vendedor ou outra função? Precisamos dessa informação para alimentar a base de conhecimento do agente e evitar alucinações.
- [x] [NOVO] Ao qualificar um lead, deseja que a etiqueta específica configurada no agente (qualification_labels) seja gravada na coluna 'labels' da tabela local de leads (banco do ZapVoice), em vez de gravar apenas o valor estático 'qualificado'?
  - Resposta: Sim, o contato deve ser criado ou atualizado no banco do ZapVoice (tabela local de leads) com as etiquetas de qualificação configuradas no agente.
- [x] [NOVO] (05/09/2026) Múltiplos Fluxos de Follow-Up por Produto: O gestor pode cadastrar múltiplos fluxos de Follow-Up independentes na integração (ex: Lowticket, Mentoria, VSL). Cada fluxo possui seus próprios passos (delays, mensagens de IA/fixas, mídias e público-alvo), compartilhando horário comercial e regras de cancelamento. A troca de fluxo de follow-up para contatos é realizada via API em disparos em massa (`POST /leads/assign-followup` ou `POST /leads/assign-funnel`), reiniciando o contato automaticamente no Passo #1 do novo produto (`followup_step = 0`). Se o contato não possuir fluxo específico atribuído, o sistema utiliza o fluxo "Padrão / Principal". Caso um fluxo seja excluído, os contatos vinculados a ele são migrados automaticamente para o fluxo padrão.
- [x] [NOVO] (05/09/2026) Ocultação da Área Inferior no Follow-Up Desativado: Quando o switch mestre de 'Follow-Up Automático' estiver desligado/desativado no formulário da integração, toda a área inferior (sub-abas de navegação, seletores de fluxos por produto, passos e regras de disparo) deve ficar completamente oculta, exibindo apenas o cabeçalho e o botão de alternância (switch), eliminando poluição visual desnecessária.




